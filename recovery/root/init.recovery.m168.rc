# Minimal LOS recovery init

on init
    # create recovery directories
    mkdir /cache
    mkdir /tmp
    mkdir /data

on post-fs
    # set permissions for recovery directories
    chmod 0777 /cache
    chmod 0777 /tmp
    chmod 0777 /data

# you can add more recovery mounts if needed
